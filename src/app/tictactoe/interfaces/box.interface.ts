/**
 * Interface with needed information to build up a BoundingBox in the game. E.g. for a card. 
 */
export interface BoundingBox {
    x: number;
    y: number;
    width: number; 
    height: number
}